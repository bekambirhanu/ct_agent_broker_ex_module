[project]
name = "ct_agent_broker_ex_module"
version = "0.1.0"
description = "Broker adapter for Exness"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["broker_exness"]
